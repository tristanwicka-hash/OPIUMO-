{
  "_comment": "All tunables live here so you never have to touch source code. Edit, save, restart the bot.",

  "jupiter": {
    "_comment": "Jupiter's public Swap API for actually executing buys/sells. These URLs could NOT be verified live from this sandbox (no network egress here) - Jupiter has moved/renamed this endpoint before (quote-api.jup.ag -> lite-api.jup.ag among others). If swaps start failing with connection errors, check https://dev.jup.ag for the current endpoint and update here - one place, no code changes needed.",
    "quoteApiUrl": "https://quote-api.jup.ag/v6/quote",
    "swapApiUrl": "https://quote-api.jup.ag/v6/swap",
    "requestTimeoutMs": 10000
  },

  "trading": {
    "_comment": "Master switch. MUST be flipped to true by hand once you have verified the filter logic yourself. See NON-NEGOTIABLES in README.md. Position sizing is risk-based (from stop-loss distance), not a flat SOL amount - set totalCapitalSol to your real trading bankroll. riskPercentPerTrade is your dial (0.5-1% per the strategy spec); there is ALSO a non-overridable hard cap (1% of capital) enforced in src/trading/sizing.ts that no config value can loosen - in practice that hard cap is what actually sizes most trades, see the comment there. paperTrading is a SECOND, independent safety net: even after enabled=true, real money is only ever at risk once paperTrading is ALSO set to false - until then every buy/sell is a simulated fill against real Jupiter prices, logged to logging.paperTradesFile instead of tradesFile. No wallet is required while paperTrading is true.",
    "enabled": false,
    "paperTrading": true,
    "totalCapitalSol": 10,
    "riskPercentPerTrade": 0.75,
    "maxOpenPositions": 5,
    "maxSlippageBps": 300,
    "_priorityFeeComment": "Priority fee (lamports) attached to swap transactions, asymmetric ON PURPOSE. A SELL that fails to land leaves you holding a position that is actively losing value - real money out the door - and sells are exactly when contention spikes, because everyone is trying to exit the same token at once. A BUY that fails to land just leaves you with no position: an opportunity cost, not a loss. Paying for buy priority also buys a speed edge this strategy does not use (see the standing 'do not compete on execution speed' decision - the filters need seconds of data, so this bot was never in the first-block race), and it would be spent on every entry attempt including the ones that would have landed anyway. buyPriorityFeeLamports is therefore 0, but the key exists so it can be tuned without a code change. 100000 lamports = 0.0001 SOL. Nothing here fires while paperTrading is true - no real transaction is ever built.",
    "sellPriorityFeeLamports": 100000,
    "buyPriorityFeeLamports": 0,
    "takeProfitLadder": [
      { "atMultipleOfEntry": 2, "sellPercentOfRemaining": 50 },
      { "atMultipleOfEntry": 5, "sellPercentOfRemaining": 25 },
      { "atMultipleOfEntry": 10, "sellPercentOfRemaining": 25 }
    ],
    "atrPeriod": 14,
    "atrStopMultiplier": 1.75,
    "fallbackStopLossPercent": -30,
    "trailingStopActivateMultiple": 2,
    "trailingStopPercent": 30,
    "timeStopHours": 48,
    "priceCheckIntervalMs": 5000,
    "maxConsecutiveSellFailures": 5,
    "sellFailureBackoffBaseMs": 30000,
    "sellFailureBackoffMaxMs": 1800000
  },

  "filters": {
    "_comment": "A token must satisfy ALL of these to PASS. Any failed rule => SKIP with a reason.",
    "minLiquiditySol": 5,
    "maxTopHolderPercent": 20,
    "maxDevWalletPercent": 10,
    "requireMintAuthorityRenounced": true,
    "requireFreezeAuthorityRenounced": true,
    "minUniqueWallets": 20,
    "minTransactionCount": 30,
    "minUniqueWalletToTxRatio": 0.15,
    "rejectRiskyTokenExtensions": true,
    "maxCreatorLpPercent": 1
  },

  "sources": {
    "watchPumpFun": true,
    "watchRaydium": true
  },

  "polling": {
    "metricsMaxAgeMs": 4000,
    "metricsFetchTimeoutMs": 8000,
    "walletActivitySampleSize": 100,
    "_concurrencyComment": "Detected tokens are processed through a bounded queue instead of all at once. Every token previously fired its full ~110-call metrics pipeline in parallel with every other, which is what produced the HTTP 429s and 20s collection times. maxConcurrentTokens is how many are worked on at a time; maxQueuedTokens caps the backlog, and the OLDEST waiting token is dropped past that (a stale launch is worth less than a fresh one). Raise maxConcurrentTokens only if your RPC provider's rate limit has headroom.",
    "maxConcurrentTokens": 2,
    "maxQueuedTokens": 50
  },

  "delayProbe": {
    "_comment": "MEASUREMENT ONLY - has zero effect on any PASS/SKIP or trading decision. The bot evaluates a token a few hundred ms after launch, when it structurally cannot yet have minUniqueWallets/minTransactionCount - nobody has had time to trade it. Rather than guess a better evaluation delay, this re-collects metrics for the same token at each age below and writes them to logs/delay-probe.jsonl, so the right delay can be read off real data later. It runs on its OWN bounded queue so measurement can never delay a live decision or reintroduce rate-limiting. sampleRate is the fraction of detected tokens probed: measuring EVERY token needs ~22 RPC calls/second, which exceeds the plan's rate limit and collapses into 429 retries and timeouts. Probing a random subset keeps every observation fully accurate (uniqueWalletCount and transactionCount are never approximated) and simply covers fewer tokens - the right trade when the goal is picking a delay from a representative sample rather than auditing every launch. Raise it if your RPC plan has headroom. Set enabled=false to switch it off.",
    "enabled": true,
    "delaysSeconds": [30, 120, 300],
    "maxConcurrentProbes": 2,
    "maxQueuedProbes": 600,
    "maxPendingTokens": 400,
    "fetchTimeoutMs": 20000,
    "sampleRate": 0.3
  },

  "logging": {
    "level": "info",
    "logDir": "logs",
    "decisionsFile": "logs/decisions.jsonl",
    "tradesFile": "logs/trades.jsonl",
    "paperTradesFile": "logs/paper-trades.jsonl",
    "perpsTradesFile": "logs/perps-trades.jsonl",
    "delayProbeFile": "logs/delay-probe.jsonl",
    "maxLogFileSizeMB": 20
  },

  "perps": {
    "_comment": "Perpetuals trading via Drift Protocol. PLUMBING ONLY - there is no entry strategy wired up yet (see README). Leverage means you can lose your whole position fast, faster than spot - keep enabled=false and env=devnet until you've read the risk engine's tests and tried it with fake devnet funds first.",
    "enabled": false,
    "env": "devnet",
    "subAccountId": 0,
    "allowedMarkets": ["SOL-PERP"],
    "maxLeverage": 3,
    "maxPositionSizeUsd": 100,
    "maxOpenPositions": 1,
    "requireStopLoss": true,
    "defaultStopLossPercent": -10,
    "defaultTakeProfitPercent": 20,
    "orderTimeoutMs": 15000
  },

  "fundingArb": {
    "_comment": "Funding Rate Arbitrage strategy: long spot (assumed already held, or deposited as Drift spot collateral yourself - this bot does NOT buy the spot leg for you, see README) + short perp, equal notional, farming the funding rate. Reuses perps.* risk gate - maxLeverage below must be <= perps.maxLeverage, and market must be in perps.allowedMarkets. NOTE: Drift settles funding roughly hourly, not the 8hr cadence common on centralized exchanges - thresholds below are expressed per-hour to match Drift's actual cadence (read live from each market, not hardcoded).",
    "enabled": false,
    "market": "SOL-PERP",
    "spotMarket": "SOL",
    "checkIntervalMinutes": 5,
    "minFundingRateHourlyPercent": 0.01,
    "minConsecutiveSettlementsToEnter": 3,
    "minConsecutiveSettlementsToExit": 3,
    "maxBasisPercent": 1.0,
    "rebalanceDriftPercent": 5,
    "maxLeverage": 2,
    "notionalUsd": 100,
    "estimatedRoundTripCostBps": 10,
    "minMarginBufferPercent": 20,
    "historyFile": "logs/funding-arb-history.json"
  }
}
